lb=int(input("Enter lower bound: "))
ub=int(input("Enter upper bound: "))
print("Prime numbers between",lb,"and",ub,"are:",end=" ")
for num in range(lb,ub+1):
    if num>1:
        for i in range(2,num):
            if(num%i)==0:
                break
        else:
            print(num,end=" ")
   
